# Task 2.1 APFS parent-link fail-closed evidence

## Bound decision

- Change: `add-codex-skill-update`
- Immutable authorization manifest:
  `7df5c5ee0d3022dbed1f19c5de9e16855982b06c20fca49b99ffc742e9c3c0ff`
- Bound helper SHA-256:
  `2823d197a8faa73c19766103ca64926dedcc670b1dcc816942075ccaa929ee43`
- Bound prestate domain SHA-256:
  `41c5747beef62fb666200730139a0195e34eab078d97b88ce2128420c522506d`
- Decision timestamp: `2026-08-04T06:43:20Z`

## Observed stop

The fixed `--launch` authorization check passed. The contained prestate check
then returned:

```text
BLOCKED_SOURCE_WORKTREE_RECOVERY: parent closure drift: /Users/elvis/file/develop/opensource/openspec-superpower-change/openspec/changes/add-codex-skill-update/approvals
```

Read-only diagnosis found exactly one parent-closure difference:

| Field | Reviewed baseline | After decision recording |
|---|---:|---:|
| `approvals/` device | `16777233` | `16777233` |
| `approvals/` inode | `168855300` | `168855300` |
| `approvals/` uid | `501` | `501` |
| `approvals/` mode | `0755` | `0755` |
| `approvals/` `st_nlink` | `7` | `8` |

The sole new immediate entry was the exact immutable manifest above. On this
bound APFS volume, adding that regular file increments the directory's observed
`st_nlink`. The old helper incorrectly required the pre-decision value after the
decision recorder had performed its authorized no-overwrite manifest write.

A disposable `/private/tmp` probe on the same local filesystem confirmed the
entry-count behavior used by the correction: empty directory `2`, one regular
file `3`, a second hard-link name `4`, removal of the transient lock name `3`,
and one remaining file plus one child directory `4`. The probe directory and
only its self-created children were removed afterward.

## Mutation boundary

After the stop, read-only checks confirmed all of the following remained absent:

- `refs/heads/add-codex-skill-update-v2`;
- `.git/logs/refs/heads/add-codex-skill-update-v2`;
- `.git/worktrees/add-codex-skill-update-v2`;
- `/Users/elvis/.config/superpowers/worktrees/openspec-superpower-change/add-codex-skill-update-v2`;
- `approvals/source-bootstrap.lock`;
- `approvals/source-bootstrap/recovery-v2-journal.jsonl`;
- `/Users/elvis/file/develop/opensource/codex-skill-update`.

The quarantined prior worktree/ref/admin identities remained unchanged. No
source state, Git ref, worktree registration, sibling source, runtime, schedule,
registry, or Bootstrap Control Root was created or mutated.

## Required correction

The failed manifest and its raw snapshots remain immutable evidence. They are
not rewritten or reused. The corrected contract/helper must bind a closed APFS
phase table for the manifest, lock, journal, final ref/reflog, admin directory,
and replacement worktree; it must pass a focused RED/GREEN regression and fresh
independent Review; and it requires a new exact user approval and new immutable
manifest before task 2.1 may run again.
