# Schema-6 RED Evidence

## Accepted Plan Preflight boundary

- accepted_review:
  `docs/design/reviews/2026-08-11-add-role-first-review-routing-plan-preflight-review.md`
- accepted_review_sha256:
  `9fc3aa76e44bd722d8c7fb57f37d2c2e042b244963fc04b4d736fadbd5f8f030`
- verdict: `PASS`
- Plan SHA-256:
  `d621b09ad030dfdfd02ad45478ad7156486fc63c392ef0816cb99bc252af6a23`
- Preflight input SHA-256:
  `13a2e1a5e1f11517c39bb9fe00e140c6e61884dedc4bf21f1ede6c3cea048f75`
- authority: source RED/GREEN execution only
- excluded authority: runtime apply/sync, Pi execution, Git, canonical state,
  archive, Envelope, publication, and completion

## Source-start inventories

These inventories were captured after the accepted Plan Preflight Review was
stored and before the first source edit. They cover each complete repository
tree except the root `.git` entry, do not follow symlinks, and contain path,
kind, mode, size, and non-sensitive SHA-256 records without file contents.

| Repository | Inventory | Mode | Records | SHA-256 |
|---|---|---:|---:|---|
| Router | `/private/tmp/add-role-first-review-routing-20260810-FPWT9V/router-tree-source-start.json` | `0600` | `323` | `7805c4c70512d28b690241073fb134e9f9b7b93ddc0c3f51ea8d320c81ed0151` |
| Companion | `/private/tmp/add-role-first-review-routing-20260810-FPWT9V/companion-tree-source-start.json` | `0600` | `29` | `737f700486279fd3d007ba8a9d6e7c96bfff18273eed41d443b6b44b355d068b` |

The first source mutation after these inventories is the creation of this
evidence file. No Router or Companion production/test source was modified
before the capture.

## RED fixture binding

| Repository | Test source SHA-256 |
|---|---|
| Router | `6cafcd6bba0415643f89c9c34f985ef285f49a147890aa0602695209420ef309` |
| Companion | `d47cf3dea0a469d82a09ce4770e398e71f050f46ce799a28d465555a9e480654` |

Both suites construct schema 6 directly from the frozen schema-5 example. The
builder performs the exact reviewer-field and readonly-field replacement before
any invalid mutation. The compact fixture uses only the approved top-level
`independent_review_not_applicable_reason` field.

## RED execution

Commands:

```text
Router: PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_workflow_rules -v
Companion: PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s /Users/elvis/file/develop/opensource/codex-brief-antigravity-review/tests -v
```

| Suite | Tests | Expected failures | Errors | Existing-test regressions |
|---|---:|---:|---:|---:|
| Router | `114` | `25` | `0` | `0` |
| Companion | `82` | `25` | `0` | `0` |

Sanitized failing test groups:

- `test_schema6_current_contract_accepts_all_four_reviewer_products`
  reached the production current-schema check for `codex`, `pi`,
  `antigravity-cli`, and `grok-cli`; production rejected each with
  `schema_version must be 4 or 5`.
- `test_schema6_current_and_legacy_validation_are_isolated` showed that the
  current API still accepts schema 4/5 and lacks the legacy-only entry point.
- `test_schema6_legacy_inventory_is_read_only_and_non_authorizing` showed that
  the production legacy inventory API is absent.
- `test_schema6_pi_is_current_only_and_cannot_backfill_legacy` showed that the
  legacy-only API is absent and schema 6 cannot yet accept Pi.
- `test_schema6_reviewer_assignment_exact_shape_fails_closed`,
  `test_schema6_standard_strict_and_compact_profiles_are_exact`, and
  `test_schema6_reviewer_assignment_is_fully_readonly` reached the production
  schema gate before the planned assignment/readonly checks.
- `test_schema6_pi_review_evidence_matches_assignment_without_promotion`
  reached the production schema gate before schema-6 parent identity binding.

There were no `KeyError`, `AttributeError`, malformed-fixture, missing-test-data,
or other setup errors in the accepted RED runs. An earlier non-evidentiary test
attempt was rejected because a literal patch marker caused a test-file syntax
error; the marker was removed and both complete suites were rerun before this
RED evidence was accepted. No raw environment data, sessions, credentials, Pi
native state, or debug traces are persisted here.

## Cross-CLI v6 / Pi RED execution

- test source:
  `tests/test_cross_cli_sync.py`
- test source SHA-256:
  `ee40263d77de7540653128ac7cb9527bfbf8e63281abddf88bc4ea4f3396212f`
- command:
  `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_cross_cli_sync.RoleFirstV6CrossCliRedTests`
- result: `6 tests / 5 expected failures / 0 errors / 1 safety assertion PASS`

Sanitized failing groups:

- `test_managed_v6_binds_exact_changed_semantics` and
  `test_managed_v6_semantics_fail_closed_on_each_critical_omission` reached the
  production managed-rule API and proved version 6/exact semantic validation
  is absent.
- `test_manifest_v6_requires_four_targets_in_exact_order` reached production
  manifest validation and was rejected because Pi is not yet a declared target.
- `test_cli_exposes_pi_prestate_probe_and_receipt_contract` proved the current
  CLI lacks Pi planning, prestate, receipt, restore/recovery/commit, and probe
  commands.
- `test_build_pi_probe_is_pure_isolated_and_fail_closed` proved the isolated
  pure probe builder is absent.
- `test_native_target_verification_never_invokes_pi_probe` passed against the
  current production `verify_target`, preserving the required separation
  between deterministic native validation and optional Pi process execution.

The accepted run contains no fixture/import/syntax error. It did not execute Pi,
read Pi native state, modify runtime targets, or persist raw subprocess output.

## Complete no-Git source-delta RED execution

- command:
  `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_cross_cli_sync.SourceDeltaTests`
- accepted RED result: `3 tests / 3 expected failures / 0 errors`
- production failure boundary: the complete-tree inventory, exact allowlist
  comparison, exclusive compare-root creation, and safe archive extraction
  functions were absent.

The three accepted failures came from explicit callable assertions for the
missing production source-delta behavior. The fixtures already contained a
hidden `.gitignore`, an existing unrelated Review, a symlink, a root `.git`
entry, and a traversal tar member. An earlier run was rejected because the new
test class was inserted before the durable-transaction class had ended, which
misassigned three existing transaction methods. The class boundary was fixed
and one affected transaction test was rerun PASS before the three-failure RED
above was accepted.

## Natural role-first forward-runner RED execution

- command:
  `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_workflow_rules.WorkflowRulesTest.test_role_first_forward_fixture_withholds_expected_from_model_prompt`
- accepted RED result: `1 test / 1 expected failure / 0 errors`
- production failure boundary: the required role-first forward runner and its
  private-oracle fixture/output-schema closure did not exist.

The RED assertion failed on the missing governed runner path, before any model
process could start. It did not execute Codex or Pi, inspect runtime/private
state, or persist raw output. Subsequent GREEN coverage separately validates
oracle withholding, exact/malformed fields, product substitution, same-instance
blocking, blocker/resume rules, and the sanitized eight-field summary.
