
ChatGPT 这 7 点都成立，尤其是冷读冲突、A/B/C 判定门槛、备份不自动清、implement 升级回灌这几条。下面是吸收全部修正后的最终版，可直接发给 Codex。

---

## 发给 Codex 的最终任务提示词

```text
任务：评估并重构我的日常开发 Skill 工作流。

核心目标：
1. 验证 Superpowers 能否被完整剔除；若验证可行则彻底剔除。
2. 在不削弱现有治理能力的前提下，吸收 mattpocock/skills 中确有价值的能力。
3. 盘点并清理全局不再使用的 Skill。
4. 评估必须允许“无法安全剔除 Superpowers”作为合法结论，不得为了完成任务而弱化阻塞项。

现有项目：
1. /Users/elvis/file/develop/opensource/openspec-superpower-change
   https://github.com/Liliang1123/openspec-superpower-change
2. /Users/elvis/file/develop/opensource/codex-brief-antigravity-review
   https://github.com/Liliang1123/codex-brief-antigravity-review

候选方案：
https://github.com/mattpocock/skills
重点研究：
grill-with-docs、to-spec、to-tickets、implement、tdd、diagnosing-bugs、code-review、handoff、setup-matt-pocock-skills。
必须阅读相关 Skill 的完整实现与文档，不要只看 README。

硬性原则：
1. 「完整剔除 Superpowers」包含两个独立验收面：
   - 源码层：两个项目内不存在 Superpowers 调用、适配、文档、测试、配置或隐式路由。
   - 运行时层：在我确认准确删除名单后，删除全局安装的 Superpowers 及其重复副本。
   源码改造完成不等于全局删除完成；全局仍存在 Superpowers 时，不得宣称“完全剔除完成”。
2. OpenSpec 默认保留为唯一权威变更契约与审批入口。仅当评估证明 to-spec 具备同等审批/门禁能力时，才讨论替换 OpenSpec。无论哪种结果，禁止同时维护两套权威 Spec。
3. codex-brief-antigravity-review 的核心 Handoff、Brief、Report、Review、证据绑定及 PASS/FAIL/BLOCKED 契约默认冻结。
   只有在证明行为与结构化契约不被削弱后，才允许进行删除失效引用、更新路由说明和回归测试等最小兼容性修改；无法证明时保持只读，并将其列为方案 A 的阻塞项。
4. 只安装最终方案真正需要的 mattpocock Skill，禁止默认安装整套。安装前必须解析完整依赖闭包、调用方式和 Codex 兼容性，不得只安装顶层 Skill 后留下缺失的下游能力。
5. mattpocock/implement 的默认 commit 行为必须移除或覆盖；commit、push 仍需我单独授权。
6. 未经我明确确认，不执行任何文件修改、安装、卸载、删除、commit、push 或全局 Skill 同步。

方案判定标准（客观门槛）：
- 选择 A（完全删除）：所有 Superpowers 能力均有等价或更强替代；不存在“暂无等价能力”项；现有门禁未被削弱；兼容、沙盘、冷读和回归验证全部通过。任何关键证据缺失时不得选择 A。
- 选择 B（最小子集）：存在不可替代项，但能够被明确隔离为可独立安装、独立触发的最小子集，且不会通过隐式路由重新激活已删除能力。
- 选择 C（保持现有）：关键门禁无法等价替代，候选方案与 Codex 不兼容，或替代后复杂度、维护成本和风险不低于现有方案。

====================
第一阶段：只读评估
====================

禁止事项：
修改现有项目文件、安装 Skill、卸载 Skill、删除 Skill、commit、push、同步全局运行时。
如需生成一次性评估产物，只能写在不修改项目和全局环境的临时隔离目录中。

必须完成：

1. 完整阅读两个现有项目的 SKILL.md、references、测试、校验脚本，输出职责边界对照表。

2. 完整阅读 mattpocock/skills 中相关 Skill 的当前实现与文档。

3. 以代码与配置中的真实引用为准，盘点当前全部 superpowers:* 能力与引用位置，建立逐项替代矩阵。
   至少覆盖以下项，并可增补实际发现的 adapter / bridge / 路由入口：
   - brainstorming
   - writing-plans
   - executing-plans / subagent-driven-development
   - test-driven-development
   - systematic-debugging
   - requesting-code-review
   - verification-before-completion
   - using-git-worktrees
   - finishing-a-development-branch
   - writing-skills

4. 对替代矩阵中的每一项，四选一判定，并给出证据：
   - 可直接由 mattpocock Skill 替代
   - 可由现有 OpenSpec、证据门禁或最终完成契约承担
   - 需要补充轻量自有实现
   - 暂无等价能力，构成完全剔除的阻塞项

5. 额外输出一张「若引入候选能力，现有门禁可能被削弱」的风险对照表，至少覆盖：
   - 变更准入与风险分级
   - OpenSpec 审批
   - 证据门禁
   - Review 失败回环
   - PASS/FAIL/BLOCKED
   - 最终完成契约
   - 外部 Agent 身份与证据可信度
   - Git commit/push 授权

6. 对比三种方案的可行性、收益、代价、迁移风险与回滚成本：
   - 方案 A：完全删除 Superpowers
   - 方案 B：仅保留经证明不可替代的最小子集
   - 方案 C：继续使用现有组合
   判定必须符合上述客观门槛。

7. 使用至少三类代表性任务做沙盘验证（只分析，不改现有项目代码）：
   - 低风险小修复
   - 普通多文件功能变更
   - 涉及 API、数据、运行时或外部 Agent 的高风险变更

8. 增加冷读测试：
   优先选取现有项目中已经存在的 OpenSpec 与任务产物；如没有合适样本，只能在不修改项目和全局环境的临时隔离目录中生成一次性评估产物。
   使用全新会话或上下文隔离的独立 Agent，仅向其提供：
   - 权威 OpenSpec
   - 派生 Tickets
   - 必要的项目规则入口
   让其输出：
   - 它理解的目标
   - 准备修改的范围
   - 每张 Ticket 的交付内容
   - 存在多种解释的位置
   - 一份可执行的实施计划
   若当前环境无法创建真正隔离的新会话或独立 Agent，必须标记为 BLOCKED，不得用当前会话假装完成冷读测试。

9. 若评估涉及从 OpenSpec 派生 Tickets：每张 Ticket 必须能关联到来源 OpenSpec 的 change-id 与批准版本。评估中需说明：当 OpenSpec 内容变化后，如何将原 Tickets 标记为过期并要求重新生成或重新审核，禁止继续执行基于旧 Spec 的 Tickets。

10. 盘点 Codex 全局已安装 Skill，输出分类清单：
   - 必须保留：系统内置、插件托管、或其他工作流仍依赖
   - 当前工作流仍有依赖
   - 能力重复
   - 已无用途
   - 可以永久删除
   对“可以永久删除”的每一项，附：删除理由、依赖检查结果、恢复方式。

11. 检查 Skill 之间的显式引用、隐式触发、全局规则、项目规则、软链接、物化副本和运行时副本；不能仅根据名称判断是否无用。

12. 若推荐引入任何 mattpocock Skill，必须解析并列出完整依赖闭包，并验证：
   - user-invoked / model-invoked 在 Codex 中是否按预期触发
   - slash command、disable-model-invocation 等机制是否兼容
   - issue tracker、临时文件和新会话接力是否可用
   - 是否存在同名 Skill、触发冲突或重复加载

第一阶段只输出：
- 明确推荐结论（A / B / C）及对应判定门槛的满足情况
- Superpowers 替代矩阵
- 阻塞项清单
- 门禁削弱风险表
- 三种方案对比
- 全局 Skill 分类清单与删除候选名单
- 依赖闭包与 Codex 兼容性检查结果
- 沙盘与冷读验证要点（若冷读 BLOCKED，明确写出）
- 收益、代价、迁移风险、回滚方式

输出完成后暂停，等待我确认实施方向。

====================
第二阶段：确认后实施
====================

仅在我明确确认方案后执行。按确认结果分支：

【若确认方案 A：完全剔除 Superpowers】
1. 删除相关 SKILL.md、references、模板、示例、测试和文档中全部 superpowers:* 调用及依赖。
2. 删除 Superpowers adapter、兼容分支和失效回退逻辑。
3. 不得出现表面删除、底层仍间接调用 Superpowers 的情况。
4. 使用 mattpocock/skills 或现有自有机制承接必要能力：
   - 需求澄清：grill-with-docs / domain-modeling（仅在领域语言或边界不清时）
   - 规格沉淀：保留 OpenSpec 作为唯一权威来源；不并行维护 to-spec 权威副本
   - 任务拆解：引入 to-tickets 的垂直切片与阻塞关系；每张 Ticket 必须记录来源 OpenSpec 的 change-id 和批准版本；OpenSpec 变更后原 Tickets 必须标记过期并重新生成或重新审核
   - 实施：支持每张 Ticket 在新窗口独立执行
   - 开发纪律：tdd、diagnosing-bugs
   - 审查：code-review，同时保留现有证据审核与失败回环
   - 完成判定：继续使用经过验证的证据门禁与最终完成契约
5. 覆盖或移除 implement 的默认 commit。若需修改 mattpocock/implement 行为，必须明确采用以下之一并记录：
   - 固定版本的本地维护副本
   - 自有 wrapper / adapter
   - 可重复应用的补丁机制
   并增加回归测试，确保后续上游更新不会重新引入自动 commit、隐式 push 或其他越权行为；记录版本来源、更新流程和回滚方式。
6. commit、push 仍需我单独授权。
7. 若某项能力没有等价替代：优先设计简洁自有替代；仍无法安全替代时，明确报告阻塞并停止，不得静默保留 Superpowers。
8. 对 openspec-superpower-change：按方案完成改造与测试更新。
9. 对 codex-brief-antigravity-review：仅在证明核心契约不被削弱后，允许删除失效引用、更新路由说明和回归测试等最小兼容性修改；无法证明时保持只读并报告为阻塞。
10. 更新规则、README、references、示例和自动化测试。
11. 执行完整验证；用 rg 等手段确认源码层不存在残留 Superpowers 引用、失效路径或悬空依赖。
12. 源码层验收通过后，再进入全局运行时清理；两者分开验收。

【若确认方案 B：仅保留最小不可替代子集】
1. 明确列出保留的 Superpowers 能力、保留理由与调用边界。
2. 其余 Superpowers 能力按方案 A 同等标准删除。
3. 保留项不得通过隐式全局规则或适配层泄漏回已删除路径。
4. 其余实施要求与方案 A 的第 4–12 条相同（在适用范围内）。

【若确认方案 C：继续使用现有组合】
1. 不删除 Superpowers。
2. 仅归档评估结论；如有必要，可做不影响行为的文档澄清。
3. 不做架构级改造，不安装整套 mattpocock skills。

全局 Skill 清理（仅在我确认准确名单后执行）：
1. 只安装最终方案实际需要的 mattpocock Skill 及其完整依赖闭包，不安装无关能力。
2. 根据第一阶段名单，列出准备永久删除的全局 Skill、删除理由、依赖检查结果和恢复方式。
3. 得到我对准确名单的确认后，再永久删除无用的个人/全局 Skill 及重复副本。
4. 不得删除系统内置、插件托管、或仍被其他工作流引用的 Skill。
5. 删除前创建可恢复备份，记录来源版本、备份路径、恢复命令和完整性校验。
6. 删除后重新扫描全局 Skill、项目规则、软链接、适配层和运行时副本，确认不存在失效触发或悬空引用。
7. 识别真实目录、软链接和物化副本，禁止因删除软链接目标而误伤仍在使用的 Skill。
8. 备份至少保留到新工作流完成实际任务验证；未经我再次确认，不自动删除备份。

最终输出必须包括：
1. 最终架构与完整调用链
2. Superpowers 替代矩阵及源码层 / 运行时层残留扫描结果
3. 修改清单与删除清单
4. 全局保留的 Skill 清单与依赖闭包
5. implement 行为覆盖方式、版本来源、回归测试与回滚方式（如适用）
6. 测试与冷读验证证据
7. 兼容性影响、剩余风险和回滚方式

再次强调：
未经我明确授权，不执行 commit 或 push。
```

---

## 相对上一版的关键变更摘要

| # | 修正点                      | 处理方式                                                      |
| - | --------------------------- | ------------------------------------------------------------- |
| 1 | 完整剔除分源码层 / 运行时层 | 写入硬性原则第 1 条，方案 A 分两段验收                        |
| 2 | 冷读与禁止写文件冲突        | 第 8 条：优先已有产物，否则临时隔离目录；无法真隔离则 BLOCKED |
| 3 | antigravity 只读 vs 方案 A  | 默认冻结核心契约；仅证明不削弱后才允许最小兼容修改            |
| 4 | Skill 依赖闭包              | 硬性原则第 4 条 + 第一阶段第 12 条                            |
| 5 | implement 升级回灌          | 方案 A 第 5 条：固定版本 / wrapper / 可重复补丁 + 回归测试    |
| 6 | A/B/C 客观门槛              | 文首「方案判定标准」；缺关键证据不得选 A                      |
| 7 | 备份不自动清                | 全局清理第 5–8 条；需再次确认才删备份                        |
| + | Ticket ↔ OpenSpec 版本绑定 | 第一阶段第 9 条 + 方案 A 任务拆解条款                         |

可以直接把上面代码块发给 Codex 做第一阶段只读评估。若 ChatGPT 还要再改措辞，优先保住这 7 条硬约束，不要被改软。
