# Tasks: add-role-first-review-routing

## 1. Proposal and approval

- [x] 1.1 Inspect the current workflow spec, product enum, Handoff identity,
  managed rule, portable manifest, Pi runtime capabilities, active changes, and
  dirty source state without modifying implementation/runtime files.
- [x] 1.2 Record the user-confirmed role-first four-product schema-6 design, the
  frozen older-schema boundary, and Codex-only control-plane authority in
  proposal/design/spec delta artifacts.
- [x] 1.3 Run `openspec validate add-role-first-review-routing --strict` and
  reconcile every proposal-stage finding.
- [x] 1.4 Obtain the Proposal Review assignment defined in `design.md`: purpose
  is complete-contract review for `PASS`/`BLOCKED`, product `codex`, role
  `independent-reviewer`, profile `control-plane-high`, a user-opened new-window
  instance distinct from the author, and Proposal-evidence-only authority.
- [x] 1.5 Obtain explicit user approval of the exact change-id
  `add-role-first-review-routing` and its current scoped contract.

## 2. Post-approval Preflight and recovery preparation

- [x] 2.1 Invoke `superpowers:writing-plans` and create one executable plan for
  Router source, Companion source, deterministic tests, four-target sync, and
  closeout; do not create a second design contract.
- [x] 2.2 Refresh applicable instructions, active Handoff inventory, repository
  status, declared runtime targets, and all source/destination preimages.
- [x] 2.3 Create fresh timestamped structured backups outside every Skill
  discovery root with mode-`0600` protection where required.
- [x] 2.4 Bind the current dirty Router bytes, clean Companion prestate, Pi
  Skill/global-rule prestate, and the remaining three runtime preimages; stop on
  any later drift.
- [x] 2.5 Obtain the Plan Preflight Review assignment defined in `design.md`,
  including non-blank purpose, product `codex`, role `independent-reviewer`,
  profile `control-plane-high`, an instance distinct from plan author/executor,
  and Preflight-evidence-only authority.

## 3. RED tests

- [x] 3.1 Add failing Router and Companion validator tests for the schema-6 `pi`
  product, product/role/profile/instance binding, user-selected product
  preservation, role/profile/instance/contract-bound Codex-only control-plane
  ownership, and frozen Pi-free schema-4/schema-5 parent-context branches.
- [x] 3.2 Add failing tests for same-instance self-review, unknown products,
  missing/extra/blank schema-6 `reviewer_assignment` fields, malformed structured
  purpose/independence, mutable assignment transitions, identity/evidence
  mismatch, missing standard/strict reviewer, old-schema Pi identities, and
  Reviewer PASS attempting canonical promotion.
- [x] 3.3 Add failing standalone prompt cases for generic “other agent”,
  user-selected Pi, new-window Codex, advisory Review, and unavailable required
  reviewer behavior.
- [x] 3.4 Add failing cross-CLI tests for a required Pi target, Pi root/path
  safety, direct native-root CLI probe rejection, isolated temporary probe
  guards, sensitive exclusions, complete v6 semantics for revised
  `CCG-001/002/010` plus `CCG-016`, portable parity, discovery evidence,
  target-local restore, and verify-all closure.
- [x] 3.5 Persist sanitized RED evidence proving failures come from the missing
  schema-6 role-first/Pi behavior rather than missing fixtures, imports, or test
  setup. RED must include standard/strict independent Review, compact
  control-plane Review, active schema-4/schema-5 drain, post-cutover old-shape
  rejection, and immutable historical treatment.

## 4. Router source implementation

- [x] 4.1 Add schema 6 with the exact four-product enum while preserving separate
  frozen schema-4/schema-5 product/assignment/evidence definitions; make current
  creation/validation/transition accept schema 6 only, route complete old schemas
  to a separate read-only non-authorizing inventory/audit path, and keep Pi
  invalid in every older parent context.
- [x] 4.2 Implement the schema-6 Handoff generator and Router validator with one
  exact always-present immutable `reviewer_assignment`: structured
  `review_purpose`, product, instance, role, profile, structured independence,
  and evidence-only result authority. Cross-check standard/strict and compact
  assignments, exact `readonly_fields`, canonical-SHA evidence binding, and every
  transition; also apply the six-field contract to standalone response routing.
- [x] 4.3 Revise `CCG-001/002/010`, add `CCG-016`, upgrade the managed
  block/manifest to v6, and require exact stable IDs `CCG-001` through
  `CCG-016` plus semantic regression checks for all four changed invariant bodies.
- [x] 4.4 Add Pi to cross-CLI target planning, root containment, safe global-rule
  handling, portable closure, deterministic target validation, isolated
  temporary capability probing with native-root denial, rollback, and verify-all
  semantics; never invoke Pi CLI against the native target root.
- [x] 4.5 Update only necessary public documentation and tests; preserve
  unrelated dirty source bytes and active change artifacts.

## 5. Companion source implementation

- [x] 5.1 Require standalone Review prompts/recommendations to state a non-blank
  Review purpose, name a concrete reviewer product, and label role, profile,
  independence, and result authority.
- [x] 5.2 Implement the same exact schema-6 `reviewer_assignment`, immutable
  binding, schema-2 source-fingerprint evidence checks, standard/strict/compact
  behavior, and four-product enum in Companion Handoff/Review templates and
  validator; current validation/transition accepts schema 6 only, while frozen
  schema-4/schema-5 parsing is isolated to read-only non-authorizing audit without Pi.
- [x] 5.3 Extend focused fixtures/tests for Pi executor/reviewer assignments,
  old-schema Pi/old-shape current rejection, product substitution,
  missing/extra/blank structured purpose or independence fields, assignment
  mutation, generic unresolved destinations, self-review, and malformed evidence.
- [x] 5.4 Keep standalone Review lightweight and prohibit it from creating or
  advancing canonical batch state.

## 6. Source verification and Review

- [x] 6.1 Run quick validators, Router/Companion project validators, both
  unittest suites, cross-skill compatibility tests, static semantic checks,
  sensitive scans, and isolated GREEN forward-tests.
- [x] 6.2 Verify all approved response-routing and Pi identity scenarios,
  including user-selected product preservation, required-reviewer BLOCKED,
  schema-6 standard/strict/compact assignments, frozen old schemas, and
  canonical-SHA binding of the immutable Reviewer Assignment.
- [x] 6.3 Obtain the Candidate source High Review assignment defined in
  `design.md`: review purpose covering actual files/diffs/validators/tests and an
  adversarial routing probe, product `codex`, role `independent-reviewer`, profile
  `control-plane-high`, a fresh instance distinct from authors/executors, and
  implementation-evidence-only authority.
- [x] 6.4 Fix every finding, refresh evidence, and repeat the same Review before
  runtime synchronization.

## 7. Old-schema drain and four-target synchronization

- [x] 7.1 Inventory every known canonical Handoff root, require every active
  schema-4/schema-5 contract to reach `complete` under the pre-upgrade runtime,
  retain a sanitized no-active-old-schema result, and repeat the inventory
  immediately before the first apply; do not migrate or resume old schemas after
  cutover.
- [x] 7.2 Generate a path/hash-only reviewed sync plan for Codex, Pi,
  Antigravity CLI, and Grok CLI from the validated canonical source.
- [x] 7.3 Obtain the Sync-plan Review assignment defined in `design.md`: purpose
  covering roots/preimages/isolation/closure/backup/restore/exclusions/order/stop
  conditions, product `codex`, role `independent-reviewer`, profile
  `control-plane-high`, an instance distinct from plan author/target executor,
  and sync-plan-evidence-only authority.
- [x] 7.4 Apply and verify Codex, Pi, Antigravity CLI, and Grok CLI one target at
  a time; restore only the current failed target and stop later targets.
- [x] 7.5 Prove all four targets have portable file parity, managed block
  v6/`CCG-001`–`CCG-016` parity, target-compatible validation, and discovery.
- [ ] 7.6 Obtain the Pi adversarial Review assignment defined in `design.md` after
  candidate Pi validation admits it; bind non-blank purpose, product `pi`, role
  `independent-reviewer`, profile `control-plane-high`, a fresh isolated session
  distinct from every Pi executor/author, input revision, complete findings, and
  governed-evidence-only authority.

## 8. Learning, final verification, and closeout

- [x] 8.1 Run Project Learning Closeout after implementation Review PASS,
  promote only confirmed non-sensitive durable invariants, and obtain the
  Learning Review assignment defined in `design.md` before final verification.
- [x] 8.2 Run fresh final verification after the last source, learning, or
  runtime mutation; persist complete four-target and assignment evidence.
- [x] 8.3 Obtain the Final High Review assignment defined in `design.md`: purpose
  covering the complete final source/runtime/evidence revision and completion
  claims, product `codex`, role `independent-reviewer`, profile
  `control-plane-high`, a user-opened new-window instance distinct from authors,
  executors, and decision owner, and final-Review-evidence-only authority.
- [x] 8.4 Reconcile this task ledger and the Superpowers plan with no unexplained
  required item.
- [ ] 8.5 Archive only when Completion Contract semantics allow it, then run
  post-archive strict validation and preserve the archive receipt.
- [ ] 8.6 Remove temporary backups only after all rollback/investigation needs
  are closed and exact cleanup targets are safely authorized.
- [x] 8.7 Keep Git staging, commit, push, publication, and every destructive
  cleanup action under separate explicit user authorization.

### Current closeout boundary — 2026-08-21 R17

Fresh R17 execution completed Tasks 7.4 and 7.5 for all four required targets;
the four receipts and `verify-all` are `PASS`, and Task 8.2 fresh source/runtime
verification is `PASS`. The isolated Pi adversarial Review required by Task 7.6
returned fail-closed `BLOCKED` because the mandated fresh HOME has no API key;
credentials/network/native-root access cannot be added to bypass isolation.
The renewed independent Final High Review records this Pi blocker; 8.3 and 8.4
are now reconciled and complete as evidence steps, but not as a completion
claim. Whole-task completion is `BLOCKED`; archive (8.5) and backup
cleanup (8.6) are not permitted until Task 7.6 and the renewed Final Review
pass. Resume condition: provide a valid isolated Pi reviewer capability (or a
governed user-approved external Pi Review artifact satisfying the exact
assignment), then rerun final verification and Final High Review.
