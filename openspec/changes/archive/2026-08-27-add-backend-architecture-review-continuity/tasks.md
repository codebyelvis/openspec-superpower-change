# Tasks: add-backend-architecture-review-continuity

## 1. Contract and Preflight

- [x] 1.1 Inspect Router routing, state, verification, Completion, project
  context, local instructions, and only the requested upstream architecture
  sources.
- [x] 1.2 Create the exact OpenSpec proposal/design/spec/tasks contract and bind
  the user's explicit implementation authorization to the change-id.
- [x] 1.3 Create the executable plan at
  `docs/superpowers/plans/2026-08-25-backend-architecture-review-continuity.md`
  with exact files, commands, rollback, stop conditions, inline Pi execution,
  no external Handoff, and no unauthorized Git steps.
- [x] 1.4 Create timestamped structured backups outside discovery roots.
- [x] 1.5 Run strict OpenSpec validation and fresh independent proposal/Plan
  Preflight Review for the original contract revision.
- [x] 1.6 Run fresh independent Plan Preflight for the current user-authorized
  proportionality/convergence revision; correct every finding before Task 4.4.
- [x] 1.7 Re-Preflight the attempt-`r2` probe revision after the preserved `r1`
  trace established Codex wraps exact commands with `/bin/zsh -c`.
- [x] 1.8 Preflight final attempt `r3` with explicit `gpt-5.6-luna` / max,
  a 1800-second timeout, fail-closed `control-plane-high` handback, and no `r4`;
  preserve `r2`, which completed four required reads but produced no action/final.
- [x] 1.9 Stop the non-converging dedicated probe audit after the user's boundary
  decision accepted minimal evidence; preserve convergence `r1` RED and
  incomplete `r2`, and do not create convergence `r3`/`r4` or another
  runner/state lifecycle.
- [x] 1.10 Run fresh independent Preflight over the minimal-evidence Plan
  revision before implementing Task 4.5.
- [x] 1.11 Attempt fresh Preflight of continuity r4 semantic evidence; retain the
  no-verdict timeout without accepting it as Review evidence.
- [x] 1.12 Run one lightweight fresh Preflight over the mode-`0600` sanitized r4
  semantic summary and its bound Plan/hash; do not load raw traces or retry r5.

## 2. RED

- [x] 2.1 Add focused failing regressions for explicit architecture triggers,
  generic Review non-triggers, specialist authority exclusions, pending-task
  continuation, canonical resume, actual-action progress, legal stop conditions,
  and existing Done criteria.
- [x] 2.2 Run the focused tests and retain expected RED evidence caused by the
  missing rules.
- [x] 2.3 Run the answer-free architecture scenarios through fresh isolated
  agents without the candidate Skill and record actual failures/rationalizations
  as sanitized RED evidence, not a hypothetical baseline.

## 3. Sibling Skill

- [x] 3.1 Create the four-file `backend-architecture-review` source with narrow
  metadata, read-only findings-first behavior, seven review dimensions, minimal
  output, and trigger examples.
- [x] 3.2 Initialize its independent local Git repository on `main`; do not stage,
  commit, add a remote, push, or publish.
- [x] 3.3 Install absent-prestate discovery symlinks for the independent source
  in all four required runtime Skill roots and verify exact resolution.
- [x] 3.4 Rerun the identical answer-free scenarios through fresh isolated
  agents with the candidate Skill; verify trigger/non-trigger, quick PASS,
  real-defect, read-only/minimal-output behavior, and run `quick_validate.py`
  through source and runtime paths.

## 4. Router implementation

- [x] 4.1 Add the thin explicit backend-architecture route and bounded evidence
  return to `SKILL.md` without changing ordinary Review or governance authority;
  retain traceability for ordinary non-backend architecture fallback to the Router.
- [x] 4.2 Add authorized execution continuity to
  `references/approved-implementation-workflow.md` without another state or Done
  contract.
- [x] 4.3 Run focused GREEN tests and refactor only while behavior stays green.
- [x] 4.4 Add focused RED regressions and retain the existing private answer-free
  `r1` baseline for conditional minimal implementation and non-converging
  Review/Fix behavior. Preserve the observed continue-and-widen failure before
  source edits; add no replacement runner.
- [x] 4.5 Add only the conditional proportionality and existing-mechanism
  convergence rules to `SKILL.md` and
  `references/approved-implementation-workflow.md`; run focused GREEN without a
  new reference, state, gate, specialist trigger, or authority.

## 5. Verification, Review, and sync

- [x] 5.1 Rerun source `quick_validate.py`, dual-parser
  `validate_core_gates.py`, strict OpenSpec validation, and the full
  dependency-free unittest suite after the last source edit.
- [x] 5.2 Rerun targeted trigger/non-trigger and continuity forward scenarios
  against the revised source; preserve distinct-thread/hash/mode evidence.
- [x] 5.3 Run distinct High Review over both repositories' actual files, complete
  diffs/status, source-to-route wiring, tests, scope, and adversarial cases,
  including one neutral non-converging Review/Fix scenario; fix, verify, and
  re-Review every finding without adding a dedicated convergence runner.
- [x] 5.4 Synchronize only the two changed portable Router files to Codex, Pi,
  Antigravity CLI, and Grok CLI after a reviewed prestate-bound plan proves the
  exact two-path mutation set per target. The current full-manifest transaction
  is ineligible if it rewrites unchanged Router/Companion/global-rule paths;
  record `BLOCKED` and stop rather than expanding this change to sync tooling.
  Historical pre-unblock state: `BLOCKED_TARGETED_TOOLING`; owner
  `control-plane-high`;
  evidence `/private/tmp/add-backend-architecture-review-continuity-20260825-sync-audit-r2/blocked-targeted-sync.json`
  SHA-256 `d76abf8ce150685cc9c70001785832e9d9c6dfaa2b9497354370ac04aeb1f48e`;
  resolved by the approved Batch-A exact two-path runtime at
  `/private/tmp/add-scoped-cross-cli-sync-plans-20260826/batch-a-runtime/a1`
  with fresh runtime Review PASS
  `/private/tmp/add-scoped-cross-cli-sync-plans-20260826/batch-a-runtime-review/a3/review.final.md`
  SHA-256 `e6856bce1f558f92d75a871adf3429aca62dd64eb860d721109d39c1df9c6165`.

## 6. Closeout

- [x] 6.1 Run Project Learning Closeout; promote only independently confirmed or
  high-severity project-local invariants. The Appendix C terminal-LF mismatch
  was classified as a single task-local evidence correction already governed
  by the approved Plan and Review; no new durable project-local Candidate Card,
  engineering invariant, or regression scope is promoted. Provenance is
  retained in the Batch-A a2 FAIL and a3 PASS Reviews:
  `/private/tmp/add-scoped-cross-cli-sync-plans-20260826/batch-a-runtime-review/a2/review.final.md`
  SHA-256 `57d09f3322a6738c924d2d76d12a11760e1a9103d6dc05a965990cf759143ab2`;
  `/private/tmp/add-scoped-cross-cli-sync-plans-20260826/batch-a-runtime-review/a3/review.final.md`
  SHA-256 `e6856bce1f558f92d75a871adf3429aca62dd64eb860d721109d39c1df9c6165`.
- [x] 6.2 Run fresh final verification after the last mutation and final Review.
  Final correction verification is recorded at
  `/private/tmp/fix-cross-cli-sync-review-findings-20260827/final-verification/current/summary.json`;
  its current SHA-256 is recorded in the bound evidence manifest. It covers the
  corrected source, four-runtime parity, strict validation, full unittest, and
  coherent managed-rule retarget rejection. The prior implementation Review is
  retained at
  `/private/tmp/fix-cross-cli-sync-review-findings-20260827/final-review/final-r1/review.final.md`
  SHA-256 `cd336bb0f30432f0261d4dc1ac86484b8b3495f806f9f7a4fbb70ba259922b81`.
  The supplemental independent Grok Review is recorded at
  `/private/tmp/fix-cross-cli-sync-review-findings-20260827/final-review/grok-r1/review.final.md`
  SHA-256 `c545bcdb8d946eb23cb4488807036696b1d0474e68d55b227a31b5113ae8b568`.
  These results are governed evidence only and do not grant Completion
  authority. The later r2 self-reference failure is retained as
  non-authorizing correction evidence.
- [x] 6.3 Reconcile this ledger, archive the change when permitted, run strict
  post-archive validation, and report Git/publication state separately.
  Controlled ledger reconciliation keeps the existing archive path and records
  the correction without a second archive operation. Post-correction strict
  validation and archive-state checks are included in the final verification
  summary above; Git commit, push, and publication remain unauthorized.
- [ ] 6.4 Remove temporary backups only after rollback needs resolve; do not
  remove unrelated or unowned objects. Deferred owner: bound Codex
  control-plane/user authorization. Resume condition: rollback and audit
  retention needs have resolved and explicit cleanup authorization is provided.
  This deferred cleanup is not a Completion requirement while retained evidence
  or backups remain required.
